version: '3'
services:
  nextcloud:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    ports:
      - 8080:80
    volumes:
      - nextcloud-data:/var/www/html/nextcloud

volumes:
  nextcloud-data:
